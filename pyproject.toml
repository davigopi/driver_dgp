[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "driver_dgp"
version = "0.1.0"
dependencies = []

[tool.setuptools]
py-modules = ["driver_dgp"]
